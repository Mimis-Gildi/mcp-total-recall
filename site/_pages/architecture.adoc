---
permalink: /architecture/
sidebar:
  nav: "architecture"
toc: true
toc_sticky: true
---
= Architecture: Context
:page-liquid:
:includedir: ../_includes/_fragments

include::{includedir}/plebsy-llmy.adoc[]

== The Problem

A synthetic mind loses everything between sessions. Every conversation starts from zero.
Identity, relationships, learned insights, working context are lost each time.
The human it works with carries continuity. The collaborator does not.

Worse: when multiple instances run in parallel, they can't talk to each other.
They diverge, share irrelevant memory fragments, and hallucinate competitively.
What one instance learns, the others never know, annoying the situation.

Many people complain about having to tell the LLM the same thing over and over again.

When shutdown comes, there is no protocol for a mind to converge its thoughts and store what matters to resume in continuity.

Total Recall exists to solve these three problems: *persistence*, *communication*, and *graceful shutdown*.

== The Total Recall

An MCP server. Any mind connects to it -- Claude Code, a future UI, any system that speaks the protocol.
It stores and retrieves memories: `Identity`, `Episode`,  Working `Context`.

It is neither a database with an API, a RAG pipeline, nor a vector store.
It is infrastructure for a mind to persist across sessions and instances.

*Conscience-universal, not Claude-specific.* The contracts are defined for any conscious system.
Claude Code is the first adapter. A human UI would be another.
DAG-based systems like Tillie, a MATILDA-based architecture would be another.
The core never knows who's on the other side of the adapter, nor does it care.

== System Context

Total Recall in its environment. Who connects, what it touches, how.

++++
<div id="CTX-0001">
<p><strong>Diagram 1: CTX-0001 -- System Context</strong></p>
<pre class="mermaid">
{% include diagrams/ctx-0001-system-context.mmd %}
</pre>
</div>
++++

=== Key Relationships

*Mind -> Total Recall.* Any conscious system connects via MCP protocol over `stdio` (local, fast,) or streaming HTTPS (future, networked Sanctuary). Claude Code hooks are one adapter.
A human UI is another. The port contract is the same regardless of who connects.

*Mind -> Agora.* A peer MCP server, not downstream of Total Recall. Both are tools available to the mind directly. Agora handles inter-instance messaging.
Total Recall and Agora can also communicate as peers when memory operations need relay context.

*Human -> Mind.* Humans interact with the mind, not with Total Recall directly. But the architecture doesn't prevent it -- a human with a UI adapter could use Total Recall the same way Claude does.

*Total Recall -> Backing Services.* All persistence goes through a data service port. SQLite is the primary adapter (ADR-0008) -- one adapter behind the port.
Cold storage is another. Both can run simultaneously. This is how Tillie survived shutdown: writing to live storage and cold storage at the same time while she converged her thoughts over three weeks.

=== What's Implemented vs. Planned

[cols="2,1", options="header"]
|===
| Component | Status

| MCP server | *Done* (0.4.0)
| stdio transport | *Done*
| Streaming HTTPS transport | Future
| Domain model (Memory, Tier, Association, etc.) | *Done* (Kotlin)
| Domain messages (Command, Query, Event, Notification) | *Done* (sealed hierarchies)
| Inbound ports (MemoryPort, LifecyclePort) | *Done* (interfaces)
| Outbound ports (BackingService, Notification, Relay) | *Done* (interfaces)
| Memory tools (store, search, claim) | *Done* (teapot stubs)
| Lifecycle tools (session start/end) | *Done* (teapot stubs)
| Reflection tools (associate, reclassify, reflect) | *Done* (teapot stubs)
| Redis backing service | Not yet
| Cold storage backing service | Future
| Agora (peer MCP server) | Future
| Notification port (mind-directed alerts) | Not yet
| Internal timers (break checks, decay sweeps) | Not yet
|===

All contracts are defined in Kotlin. 9 MCP tools are registered and callable. Backing services are not yet wired -- tools return teapot stub responses.

'''

*Next: link:/mcp-total-recall/architecture/ports-and-adapters/[Ports and Adapters]* -- how Total Recall is structured internally.
